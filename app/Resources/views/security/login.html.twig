{% extends 'layout.html.twig' %}

{% block body %}
    <div class="login-page">
        <h1 class="text-center login-title">Sign in</h1>
        {% for flash_message in app.session.flashbag.get('error') %}
            <div class="alert alert-danger">
                <b>Error!</b>
                {{ flash_message.messageKey|trans(flash_message.messageData, 'security') }}
            </div>
        {% endfor %}
            <form class="form-signin" method="POST" action="{{ path('login') }}">
                <span id="reauth-email" class="reauth-email"></span>
                <input type="text" class="form-control" placeholder="Email" name="_username" value="{{ last_username}}" required autofocus>
                <input type="password" name="_password" class="form-control" placeholder="Password" required>

                <button class="btn btn-lg btn-primary btn-block" type="submit">
                    Sign in</button>
                <label class="checkbox pull-left">
                    <input type="checkbox" value="remember-me">
                    Remember me
                </label>
                <a href="#" class="pull-right need-help">Need help? </a><span class="clearfix"></span>

                <input type="hidden" name="_target_path" value="/" />
            </form>
    </div>
{% endblock %}